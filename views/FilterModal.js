import React from 'react';
import {Layout, Text} from '@ui-kitten/components';

const FilterModal = () => {
  return (
    <Layout>
      <Text>Welcome to modal</Text>
    </Layout>
  );
};

export default FilterModal;
